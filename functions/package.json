{
  "name": "functions",
  "version": "1.0.0",
  "scripts": {
    "lint": "eslint . || echo 'no lint run'",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "build": "npm run lint"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.1.1",
    "nodemailer": "^6.9.0"
  }
}
